# Migration `20200604094302-remove-vote-counts`

This migration has been generated by Yan Takushevich at 6/4/2020, 9:43:02 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Video" DROP COLUMN "downVoteCount",
DROP COLUMN "upVoteCount";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200603171016-separate-upvote-and-downvote-count..20200604094302-remove-vote-counts
--- datamodel.dml
+++ datamodel.dml
@@ -4,27 +4,25 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Video {
-  ytId          String     @id
-  etag          String
-  snippet       Json
-  complexity    Complexity
-  topic         Topic      @relation(fields: [topicId], references: [id])
-  topicId       Int
-  tags          Tag[]      @relation(references: [id])
-  votes         Vote[]
-  downVoteCount Int?       @default(0)
-  upVoteCount   Int?       @default(0)
-  uploader      User       @relation("UserVideos", fields: [uploaderId], references: [uid])
-  uploaderId    String
-  bookmarkers   User[]     @relation("UserBookmarks")
-  language      Language   @relation(fields: [languageId], references: [id])
-  languageId    Int
+  ytId        String     @id
+  etag        String
+  snippet     Json
+  complexity  Complexity
+  topic       Topic      @relation(fields: [topicId], references: [id])
+  topicId     Int
+  tags        Tag[]      @relation(references: [id])
+  votes       Vote[]
+  uploader    User       @relation("UserVideos", fields: [uploaderId], references: [uid])
+  uploaderId  String
+  bookmarkers User[]     @relation("UserBookmarks")
+  language    Language   @relation(fields: [languageId], references: [id])
+  languageId  Int
 }
 model User {
   uid       String  @id
```


